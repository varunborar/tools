export const metadata = {
  title: "Base64 Encoder/Decoder",
  description: "Encode and decode Base64 text safely (UTF-8 compatible).",
  alternates: { canonical: "/security/base64" },
  openGraph: {
    title: "Base64 Encoder/Decoder",
    description: "Encode and decode Base64 text safely (UTF-8 compatible).",
    url: "/security/base64",
  },
};

export default function Base64Layout({ children }) {
  return children;
}
