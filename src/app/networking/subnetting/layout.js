export const metadata = {
  title: "Subnetting (CIDR)",
  description: "Calculate subnet divisions, masks, ranges and host counts with ease.",
  alternates: { canonical: "/networking/subnetting" },
  openGraph: {
    title: "Subnetting (CIDR)",
    description: "Calculate subnet divisions, masks, ranges and host counts with ease.",
    url: "/networking/subnetting",
  },
};

export default function SubnettingLayout({ children }) {
  return children;
}
